<a href="{{ route('home') }}" class="{{ $wrapper ?? 'inline-flex items-center shrink-0' }}" aria-label="Falcore home">
    <img
        src="{{ asset('images/falcore-logo.png') }}"
        alt="Falcore — The core of Every Journey"
        class="{{ $class ?? 'h-11 sm:h-12 w-auto' }}"
    >
</a>
